---
breadcrumbs: true
linkTitle: Configuration
title: Configuration Reference
weight: 3
---

After running ```make bootstrap``` default configuration files are added to the ```user_config``` folder. At a minimum
you must configure the [config.yml](reference/config.yml) and [config_platform.yml](reference/config_platform.yml)
files
before you
can generate boxart
